@model List<BankApp.Client.Dto.ApplicationDto>

@{
    ViewData["Title"] = "All Applications";
}

<style>
    :root {
        --bank-primary: #1e3a5f;
        --bank-secondary: #2c5f8d;
        --bank-accent: #3498db;
        --bank-success: #27ae60;
        --bank-warning: #f39c12;
        --bank-danger: #e74c3c;
        --bank-info: #16a085;
        --bank-light: #f8f9fa;
        --bank-border: #e1e8ed;
        --bank-shadow: rgba(30, 58, 95, 0.08);
    }

    .applications-container {
        background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
        min-height: calc(100vh - 56px - 50px);
        padding: 20px 0;
    }

    .page-header {
        background: white;
        border-radius: 10px;
        padding: 16px 20px;
        margin-bottom: 20px;
        box-shadow: 0 2px 8px var(--bank-shadow);
        border-left: 4px solid var(--bank-accent);
    }

        .page-header h2 {
            color: var(--bank-primary);
            font-weight: 700;
            font-size: 1.4rem;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

            .page-header h2 i {
                color: var(--bank-accent);
                font-size: 1.3rem;
            }

    .page-subtitle {
        color: #6c757d;
        margin-top: 4px;
        font-size: 0.85rem;
    }

    .stats-overview {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 16px;
        margin-bottom: 20px;
    }

    .stat-card {
        background: white;
        border-radius: 10px;
        padding: 14px 18px;
        box-shadow: 0 2px 10px var(--bank-shadow);
        border-left: 3px solid;
        transition: all 0.3s ease;
    }

        .stat-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 16px rgba(30, 58, 95, 0.1);
        }

        .stat-card.pending {
            border-left-color: var(--bank-warning);
        }

        .stat-card.approved {
            border-left-color: var(--bank-success);
        }

        .stat-card.rejected {
            border-left-color: var(--bank-danger);
        }

        .stat-card.total {
            border-left-color: var(--bank-accent);
        }

    .stat-label {
        color: #6c757d;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.3px;
        margin-bottom: 6px;
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .stat-value {
        color: var(--bank-primary);
        font-size: 1.6rem;
        font-weight: 700;
        margin: 0;
    }

    .applications-card {
        background: white;
        border-radius: 10px;
        border: 1px solid var(--bank-border);
        box-shadow: 0 2px 10px var(--bank-shadow);
        overflow: hidden;
    }

    .applications-card-header {
        background: linear-gradient(135deg, var(--bank-light) 0%, #ffffff 100%);
        padding: 14px 18px;
        border-bottom: 2px solid var(--bank-border);
    }

        .applications-card-header h5 {
            margin: 0;
            color: var(--bank-primary);
            font-weight: 700;
            font-size: 1.05rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .applications-card-header h5 i {
                color: var(--bank-accent);
            }

    .applications-card-body {
        padding: 18px;
    }

    .applications-table {
        width: 100%;
        font-size: 0.85rem;
    }

        .applications-table thead th {
            background: linear-gradient(135deg, var(--bank-primary) 0%, var(--bank-secondary) 100%);
            color: white;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.3px;
            padding: 10px 8px;
            border: none;
            white-space: nowrap;
        }

            .applications-table thead th:first-child {
                border-top-left-radius: 6px;
            }

            .applications-table thead th:last-child {
                border-top-right-radius: 6px;
            }

        .applications-table tbody tr {
            transition: all 0.2s ease;
            border-bottom: 1px solid var(--bank-border);
        }

            .applications-table tbody tr:hover {
                background: linear-gradient(90deg, rgba(52, 152, 219, 0.05) 0%, rgba(30, 58, 95, 0.03) 100%);
            }

        .applications-table tbody td {
            padding: 10px 8px;
            vertical-align: middle;
            color: var(--bank-primary);
        }

        .applications-table tbody tr:last-child {
            border-bottom: none;
        }

    .application-id {
        font-weight: 600;
        color: var(--bank-accent);
        font-family: 'Courier New', monospace;
        font-size: 0.8rem;
    }

    .sensitive-data {
        font-family: 'Courier New', monospace;
        letter-spacing: 0.5px;
        color: var(--bank-primary);
        font-weight: 500;
        font-size: 0.8rem;
    }

    .status-badge {
        padding: 4px 10px;
        border-radius: 14px;
        font-weight: 600;
        font-size: 0.75rem;
        display: inline-flex;
        align-items: center;
        gap: 4px;
        white-space: nowrap;
    }

    .badge-pending {
        background: rgba(243, 156, 18, 0.15);
        color: var(--bank-warning);
    }

    .badge-approved {
        background: rgba(39, 174, 96, 0.15);
        color: var(--bank-success);
    }

    .badge-rejected {
        background: rgba(231, 76, 60, 0.15);
        color: var(--bank-danger);
    }

    .account-type-badge {
        padding: 4px 10px;
        border-radius: 6px;
        font-weight: 600;
        font-size: 0.75rem;
        display: inline-flex;
        align-items: center;
        gap: 4px;
    }

    .account-savings {
        background: rgba(33, 150, 243, 0.1);
        color: #1976d2;
    }

    .account-current {
        background: rgba(39, 174, 96, 0.1);
        color: var(--bank-success);
    }

    .btn-view {
        background: linear-gradient(135deg, var(--bank-info) 0%, #138d75 100%);
        border: none;
        color: white;
        padding: 6px 12px;
        border-radius: 6px;
        font-weight: 600;
        font-size: 0.75rem;
        transition: all 0.2s ease;
        display: inline-flex;
        align-items: center;
        gap: 4px;
        text-decoration: none;
    }

        .btn-view:hover {
            transform: translateY(-2px);
            box-shadow: 0 3px 10px rgba(22, 160, 133, 0.25);
            color: white;
            text-decoration: none;
        }

    .empty-state {
        text-align: center;
        padding: 50px 24px;
    }

    .empty-state-icon {
        width: 80px;
        height: 80px;
        background: linear-gradient(135deg, var(--bank-light) 0%, #e9ecef 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 16px;
    }

        .empty-state-icon i {
            font-size: 2.5rem;
            color: var(--bank-accent);
        }

    .empty-state h5 {
        color: var(--bank-primary);
        font-weight: 600;
        margin-bottom: 8px;
        font-size: 1.1rem;
    }

    .empty-state p {
        color: #6c757d;
        font-size: 0.9rem;
    }

    .dataTables_wrapper .dataTables_length select,
    .dataTables_wrapper .dataTables_filter input {
        border: 1px solid var(--bank-border);
        border-radius: 6px;
        padding: 6px 10px;
        transition: all 0.2s ease;
        font-size: 0.85rem;
    }

        .dataTables_wrapper .dataTables_length select:focus,
        .dataTables_wrapper .dataTables_filter input:focus {
            border-color: var(--bank-accent);
            outline: none;
            box-shadow: 0 0 0 2px rgba(52, 152, 219, 0.1);
        }

    .dataTables_wrapper .dataTables_paginate .paginate_button {
        border-radius: 6px;
        padding: 6px 12px;
        margin: 0 3px;
        border: 1px solid var(--bank-border);
        background: white;
        color: var(--bank-primary);
        transition: all 0.2s ease;
        font-size: 0.85rem;
    }

        .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
            background: linear-gradient(135deg, var(--bank-primary) 0%, var(--bank-secondary) 100%);
            color: white !important;
            border-color: var(--bank-primary);
        }

        .dataTables_wrapper .dataTables_paginate .paginate_button.current {
            background: linear-gradient(135deg, var(--bank-primary) 0%, var(--bank-secondary) 100%);
            color: white !important;
            border-color: var(--bank-primary);
        }

    .dataTables_wrapper .dataTables_info {
        color: #6c757d;
        font-size: 0.8rem;
        padding: 10px 0;
    }

    .dataTables_wrapper .dataTables_length,
    .dataTables_wrapper .dataTables_filter {
        margin-bottom: 12px;
    }

    @@media (max-width: 768px) {
        .applications-container {
            padding: 15px 0;
        }

        .page-header {
            padding: 12px 16px;
        }

            .page-header h2 {
                font-size: 1.2rem;
            }

        .stats-overview {
            grid-template-columns: 1fr;
        }

        .applications-card-body {
            padding: 12px;
        }

        .applications-table {
            font-size: 0.8rem;
        }

            .applications-table thead th,
            .applications-table tbody td {
                padding: 8px 6px;
            }
    }
</style>

<div class="applications-container">
    <div class="container-fluid">
        <div class="page-header">
            <h2>
                <i class="fas fa-folder-open"></i>
                All Applications
            </h2>
            <p class="page-subtitle">Review and manage all customer account applications</p>
        </div>

        @if (Model.Any())
        {
            var pendingCount = Model.Count(a => a.Status == 1);
            var approvedCount = Model.Count(a => a.Status == 2);
            var rejectedCount = Model.Count(a => a.Status == 3);
            var totalCount = Model.Count();

            <div class="stats-overview">
                <div class="stat-card total">
                    <div class="stat-label">
                        <i class="fas fa-list"></i> Total Applications
                    </div>
                    <h3 class="stat-value">@totalCount</h3>
                </div>
                <div class="stat-card pending">
                    <div class="stat-label">
                        <i class="fas fa-clock"></i> Pending Review
                    </div>
                    <h3 class="stat-value">@pendingCount</h3>
                </div>
                <div class="stat-card approved">
                    <div class="stat-label">
                        <i class="fas fa-check-circle"></i> Approved
                    </div>
                    <h3 class="stat-value">@approvedCount</h3>
                </div>
                <div class="stat-card rejected">
                    <div class="stat-label">
                        <i class="fas fa-times-circle"></i> Rejected
                    </div>
                    <h3 class="stat-value">@rejectedCount</h3>
                </div>
            </div>
        }

        <div class="row">
            <div class="col-12">
                <div class="applications-card">
                    <div class="applications-card-header">
                        <h5><i class="fas fa-table"></i> Applications List</h5>
                    </div>
                    <div class="applications-card-body">
                        @if (Model.Any())
                        {
                            <div class="table-responsive">
                                <table class="applications-table table" id="applicationsTable">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Full Name</th>
                                            <th>Date of Birth</th>
                                            <th>Mobile</th>
                                            <th>PAN</th>
                                            <th>Account Type</th>
                                            <th>Status</th>
                                            <th>Applied Date</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var app in Model)
                                        {
                                            <tr>
                                                <td>
                                                    <span class="application-id">#@app.ApplicationID</span>
                                                </td>
                                                <td>
                                                    <strong>@app.FullName</strong>
                                                </td>
                                                <td>
                                                    <span>@app.DateOfBirth.ToString("dd MMM yyyy")</span>
                                                </td>
                                                <td>
                                                    <span class="sensitive-data">@app.MobileNumber</span>
                                                </td>
                                                <td>
                                                    <span class="sensitive-data">@app.PAN</span>
                                                </td>
                                                <td>
                                                    @if (app.AccountTypeID == 1)
                                                    {
                                                        <span class="account-type-badge account-savings">
                                                            <i class="fas fa-piggy-bank"></i> Savings
                                                        </span>
                                                    }
                                                    else
                                                    {
                                                        <span class="account-type-badge account-current">
                                                            <i class="fas fa-briefcase"></i> Current
                                                        </span>
                                                    }
                                                </td>
                                                <td>
                                                    @if (app.Status == 1)
                                                    {
                                                        <span class="status-badge badge-pending">
                                                            <i class="fas fa-clock"></i> Pending
                                                        </span>
                                                    }
                                                    else if (app.Status == 2)
                                                    {
                                                        <span class="status-badge badge-approved">
                                                            <i class="fas fa-check-circle"></i> Approved
                                                        </span>
                                                    }
                                                    else
                                                    {
                                                        <span class="status-badge badge-rejected">
                                                            <i class="fas fa-times-circle"></i> Rejected
                                                        </span>
                                                    }
                                                </td>
                                                <td>
                                                    <div>@app.ApplicationDate.ToString("dd MMM yyyy")</div>
                                                    <small style="color: #6c757d; font-size: 0.75rem;">@app.ApplicationDate.ToString("HH:mm")</small>
                                                </td>
                                                <td>
                                                    <a href="@Url.Action("Details", "Application", new { id = app.ApplicationID })" class="btn-view">
                                                        <i class="fas fa-eye"></i> View
                                                    </a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <div class="empty-state">
                                <div class="empty-state-icon">
                                    <i class="fas fa-folder-open"></i>
                                </div>
                                <h5>No Applications Found</h5>
                                <p>There are currently no customer applications in the system. New applications will appear here when customers submit them.</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#applicationsTable').DataTable({
                "pageLength": 25,
                "order": [[7, "desc"]],
                "language": {
                    "search": "Search applications:",
                    "lengthMenu": "Show _MENU_ applications per page",
                    "info": "Showing _START_ to _END_ of _TOTAL_ applications",
                    "infoEmpty": "No applications available",
                    "infoFiltered": "(filtered from _MAX_ total applications)",
                    "paginate": {
                        "first": "First",
                        "last": "Last",
                        "next": "Next",
                        "previous": "Prev"
                    }
                },
                "responsive": true,
                "dom": '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>rtip'
            });
        });
    </script>
}
