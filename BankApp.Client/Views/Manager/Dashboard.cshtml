@{
    ViewData["Title"] = "Manager Dashboard";
    var pendingApplications = ViewBag.PendingApplications;
    var pendingTransactions = ViewBag.PendingTransactions;
}

<style>
    :root {
        --bank-primary: #1e3a5f;
        --bank-secondary: #2c5f8d;
        --bank-accent: #3498db;
        --bank-success: #27ae60;
        --bank-warning: #f39c12;
        --bank-danger: #e74c3c;
        --bank-info: #16a085;
        --bank-light: #f8f9fa;
        --bank-border: #e1e8ed;
        --bank-shadow: rgba(30, 58, 95, 0.08);
    }

    .manager-dashboard-container {
        background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
        min-height: calc(100vh - 56px - 50px);
        padding: 20px 0;
    }

    /* Welcome Header */
    .dashboard-welcome {
        background: white;
        border-radius: 10px;
        padding: 16px 20px;
        margin-bottom: 20px;
        box-shadow: 0 2px 8px var(--bank-shadow);
        border-left: 4px solid var(--bank-accent);
    }

        .dashboard-welcome h2 {
            color: var(--bank-primary);
            font-weight: 700;
            font-size: 1.4rem;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

            .dashboard-welcome h2 i {
                color: var(--bank-accent);
                font-size: 1.3rem;
            }

    .welcome-subtitle {
        color: #6c757d;
        margin-top: 4px;
        font-size: 0.85rem;
    }

    /* Action Cards Grid */
    .action-cards-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 16px;
        margin-bottom: 20px;
    }

    .action-card {
        border-radius: 12px;
        border: none;
        overflow: hidden;
        transition: all 0.3s ease;
        box-shadow: 0 2px 10px var(--bank-shadow);
        position: relative;
        background: white;
    }

        .action-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 6px 20px rgba(30, 58, 95, 0.12);
        }

        .action-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100px;
            height: 100px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(30%, -30%);
        }

    .action-card-warning {
        background: linear-gradient(135deg, var(--bank-warning) 0%, #e67e22 100%);
        color: white;
    }

    .action-card-info {
        background: linear-gradient(135deg, var(--bank-info) 0%, #138d75 100%);
        color: white;
    }

    .action-card-success {
        background: linear-gradient(135deg, var(--bank-success) 0%, #229954 100%);
        color: white;
    }

    .action-card-primary {
        background: linear-gradient(135deg, var(--bank-primary) 0%, var(--bank-secondary) 100%);
        color: white;
    }

    .action-card .card-body {
        padding: 18px 20px;
        position: relative;
        z-index: 1;
    }

    .card-content-wrapper {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 14px;
    }

    .card-info h6 {
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        opacity: 0.9;
        margin-bottom: 6px;
    }

    .card-info h3 {
        font-size: 2rem;
        font-weight: 700;
        margin: 0;
        line-height: 1;
    }

    .card-icon {
        font-size: 2.5rem;
        opacity: 0.3;
    }

    .action-card .btn-light {
        background: rgba(255, 255, 255, 0.95);
        border: none;
        padding: 8px 16px;
        font-weight: 600;
        font-size: 0.85rem;
        border-radius: 6px;
        transition: all 0.2s ease;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        display: inline-flex;
        align-items: center;
        gap: 6px;
        text-decoration: none;
    }

    .action-card-warning .btn-light {
        color: var(--bank-warning);
    }

    .action-card-info .btn-light {
        color: var(--bank-info);
    }

    .action-card-success .btn-light {
        color: var(--bank-success);
    }

    .action-card-primary .btn-light {
        color: var(--bank-primary);
    }

    .action-card .btn-light:hover {
        background: white;
        transform: translateX(3px);
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.12);
        text-decoration: none;
    }

    /* Quick Actions Section */
    .quick-actions-section {
        background: white;
        border-radius: 12px;
        padding: 28px;
        box-shadow: 0 2px 10px var(--bank-shadow);
        text-align: center;
        border: 1px solid var(--bank-border);
    }

    .quick-actions-icon {
        width: 80px;
        height: 80px;
        background: linear-gradient(135deg, var(--bank-light) 0%, #e9ecef 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 16px;
    }

        .quick-actions-icon i {
            font-size: 2.5rem;
            color: var(--bank-accent);
        }

    .quick-actions-section h4 {
        color: var(--bank-primary);
        font-weight: 700;
        margin-bottom: 8px;
        font-size: 1.2rem;
    }

    .quick-actions-section p {
        color: #6c757d;
        font-size: 0.9rem;
        line-height: 1.5;
        max-width: 600px;
        margin: 0 auto 20px;
    }

    /* Quick Links */
    .quick-links {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 12px;
        margin-top: 20px;
    }

    .quick-link-card {
        background: linear-gradient(135deg, var(--bank-light) 0%, #e9ecef 100%);
        border: 2px solid var(--bank-border);
        border-radius: 10px;
        padding: 16px;
        transition: all 0.3s ease;
        text-decoration: none;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
    }

        .quick-link-card:hover {
            border-color: var(--bank-accent);
            background: white;
            transform: translateY(-3px);
            box-shadow: 0 4px 12px rgba(52, 152, 219, 0.12);
            text-decoration: none;
        }

    .quick-link-icon {
        width: 50px;
        height: 50px;
        background: white;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
    }

    .quick-link-card:hover .quick-link-icon {
        background: linear-gradient(135deg, var(--bank-accent) 0%, var(--bank-secondary) 100%);
    }

    .quick-link-icon i {
        font-size: 1.5rem;
        color: var(--bank-accent);
        transition: all 0.3s ease;
    }

    .quick-link-card:hover .quick-link-icon i {
        color: white;
    }

    .quick-link-text {
        color: var(--bank-primary);
        font-weight: 600;
        font-size: 0.85rem;
        text-align: center;
    }

    .quick-link-card:hover .quick-link-text {
        color: var(--bank-accent);
    }

    /* Alert Badge */
    .alert-badge {
        position: absolute;
        top: 12px;
        right: 12px;
        background: rgba(255, 255, 255, 0.25);
        color: white;
        padding: 4px 10px;
        border-radius: 14px;
        font-size: 0.75rem;
        font-weight: 700;
        z-index: 2;
    }

    @@media (max-width: 768px) {
        .manager-dashboard-container {
            padding: 15px 0;
        }

        .dashboard-welcome {
            padding: 12px 16px;
        }

            .dashboard-welcome h2 {
                font-size: 1.2rem;
            }

        .action-cards-grid {
            grid-template-columns: 1fr;
        }

        .quick-links {
            grid-template-columns: repeat(2, 1fr);
        }

        .card-info h3 {
            font-size: 1.6rem;
        }

        .card-icon {
            font-size: 2rem;
        }

        .quick-actions-section {
            padding: 20px;
        }
    }
</style>

<div class="manager-dashboard-container">
    <div class="container-fluid">
        <!-- Welcome Header -->
        <div class="dashboard-welcome">
            <h2>
                <i class="fas fa-tachometer-alt"></i>
                Manager Dashboard
            </h2>
            <p class="welcome-subtitle">Oversee applications, transactions, and customer management operations</p>
        </div>

        <!-- Action Cards -->
        <div class="action-cards-grid">
            <div class="action-card action-card-warning">
                @if (pendingApplications > 0)
                {
                    <span class="alert-badge">@pendingApplications Pending</span>
                }
                <div class="card-body">
                    <div class="card-content-wrapper">
                        <div class="card-info">
                            <h6>Pending Applications</h6>
                            <h3>@pendingApplications</h3>
                        </div>
                        <div>
                            <i class="fas fa-file-alt card-icon"></i>
                        </div>
                    </div>
                    <a href="@Url.Action("PendingApplications")" class="btn btn-light">
                        View Applications <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </div>

            <div class="action-card action-card-info">
                @if (pendingTransactions > 0)
                {
                    <span class="alert-badge">@pendingTransactions Pending</span>
                }
                <div class="card-body">
                    <div class="card-content-wrapper">
                        <div class="card-info">
                            <h6>Pending Transactions</h6>
                            <h3>@pendingTransactions</h3>
                        </div>
                        <div>
                            <i class="fas fa-exchange-alt card-icon"></i>
                        </div>
                    </div>
                    <a href="@Url.Action("PendingTransactions")" class="btn btn-light">
                        View Transactions <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </div>

            <div class="action-card action-card-success">
                <div class="card-body">
                    <div class="card-content-wrapper">
                        <div class="card-info">
                            <h6>All Applications</h6>
                            <h3><i class="fas fa-list"></i></h3>
                        </div>
                        <div>
                            <i class="fas fa-folder-open card-icon"></i>
                        </div>
                    </div>
                    <a href="@Url.Action("Applications")" class="btn btn-light">
                        View All <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </div>

            <div class="action-card action-card-primary">
                <div class="card-body">
                    <div class="card-content-wrapper">
                        <div class="card-info">
                            <h6>Customers</h6>
                            <h3><i class="fas fa-users"></i></h3>
                        </div>
                        <div>
                            <i class="fas fa-user-tie card-icon"></i>
                        </div>
                    </div>
                    <a href="@Url.Action("Customers")" class="btn btn-light">
                        View Customers <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </div>
        </div>

        <!-- Quick Actions Section -->
        <div class="row">
            <div class="col-12">
                <div class="quick-actions-section">
                    <div class="quick-actions-icon">
                        <i class="fas fa-tasks"></i>
                    </div>
                    <h4>Management Hub</h4>
                    <p>Access all management functions from this central dashboard. Use the cards above for quick access to pending items, or explore additional options below.</p>

                    <div class="quick-links">
                        <a href="@Url.Action("PendingApplications")" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-clipboard-check"></i>
                            </div>
                            <span class="quick-link-text">Review Applications</span>
                        </a>

                        <a href="@Url.Action("PendingTransactions")" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-money-check-alt"></i>
                            </div>
                            <span class="quick-link-text">Approve Transactions</span>
                        </a>

                        <a href="@Url.Action("Applications")" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-archive"></i>
                            </div>
                            <span class="quick-link-text">All Applications</span>
                        </a>

                        <a href="@Url.Action("Customers")" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-user-friends"></i>
                            </div>
                            <span class="quick-link-text">Customer Directory</span>
                        </a>

                        <a href="@Url.Action("Transactions", "Manager")" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-receipt"></i>
                            </div>
                            <span class="quick-link-text">All Transactions</span>
                        </a>

                        <a href="#" class="quick-link-card">
                            <div class="quick-link-icon">
                                <i class="fas fa-chart-line"></i>
                            </div>
                            <span class="quick-link-text">Reports & Analytics</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
